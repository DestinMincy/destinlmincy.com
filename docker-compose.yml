services:
  postgres:
    image: postgres:15-alpine
    container_name: destinlmincy-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-destinlmincy_dev}
      POSTGRES_USER: ${POSTGRES_USER:-destinlmincy}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-destinlmincy_dev_password}
    ports:
      - "5434:5432"
    volumes:
      - destinlmincy_site_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  destinlmincy_site_postgres_data:
